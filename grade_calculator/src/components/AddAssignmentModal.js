import { useState } from "react";

function AddAssignmentModal() {
    return(
        <div className="modal">
            <h2>Add Assignment</h2><button className="delete">x</button>

            <button>Add Assignment</button>
        </div>
    );
}

export default AddAssignmentModal;